#PROGRAM
	BREQ $argc 2 LSTART
	PRINT "Wrong number of arguments "
	RETURN
LSTART	PRINT "Init: setting $free to " $argv[1]
    STORE $mtx 1
	STORE $free $argv[1]
	PRINT "Init: setting $full to " 0
	STORE $full 0
	RETURN
